<?xml version="1.0" encoding="UTF-8"?>

<!-- 告知Spring生产那些组件 -->
<beans 	xmlns="http://www.springframework.org/schema/beans"
          xmlns:tx="http://www.springframework.org/schema/tx"
          xmlns:context="http://www.springframework.org/schema/context"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://www.springframework.org/schema/beans
							http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
							http://www.springframework.org/schema/tx
							http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
							http://www.springframework.org/schema/context
							http://www.springframework.org/schema/context/spring-context-3.2.xsd">

    <!-- 引入外部文件 -->
    <context:property-placeholder location="classpath:jdbc.properties"/>

    <!-- 连接池设置 -->
    <bean id="ds" class="com.alibaba.druid.pool.DruidDataSource" init-method="init" destroy-method="close">
        <!-- 连接参数 -->
        <property name="url" value="${mysql.url}"/>
        <property name="username" value="${mysql.username}"/>
        <property name="password" value="${mysql.password}"/>

        <!-- 连接池边界 -->
        <property name="initialSize" value="${mysql.min}"/>
        <property name="minIdle" value="${mysql.min}"/>
        <property name="maxActive" value="${mysql.max}"/>

        <!-- 最大等待时间 -->
        <property name="maxWait" value="${mysql.maxWait}"/>

        <!-- 连接测试 -->
        <property name="validationQuery" value="select 1 from dual"/>
    </bean>

    <!-- SqlSessionFactory -->
    <bean id="ssf" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- 数据源 -->
        <property name="dataSource" ref="ds"/>

        <!-- DAO映射文件位置 -->
        <property name="mapperLocations">
            <list>
                <value>classpath:com/demo/dao/impl/*</value>
            </list>
        </property>

        <!-- 实体别名 -->
        <property name="typeAliasesPackage" value="com.baizhi.entity"/>
    </bean>

    <!-- 定制DAO -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!-- 从SqlSessionFactory获取SQLSession交给DAO -->
        <property name="sqlSessionFactoryBeanName" value="ssf"/>

        <!-- DAO位置 -->
        <property name="basePackage" value="com.baizhi.dao"/>
    </bean>

    <!-- 事务管理器 -->
    <bean id="tx" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="ds"/>
    </bean>

    <!-- 配置注解驱动 -->
    <!-- 告知Spring在xx包存在注解需要解析 -->
    <context:component-scan base-package="com.demo.service.impl"/>

    <!-- 告知Spring @transactional要切入的事务逻辑 -->
    <tx:annotation-driven transaction-manager="tx"/>
</beans>
